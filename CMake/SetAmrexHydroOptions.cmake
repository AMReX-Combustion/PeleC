set(HYDRO_SPACEDIM "${PELEC_DIM}" CACHE STRING "Number of physical dimensions" FORCE)
set(HYDRO_MPI ${PELEC_ENABLE_MPI})
set(HYDRO_OMP ${PELEC_ENABLE_OPENMP})
set(HYDRO_EB ${PELEC_ENABLE_AMREX_EB})

if(PELEC_ENABLE_CUDA)
  set(HYDRO_GPU_BACKEND CUDA CACHE STRING "AMReX-Hydro GPU type" FORCE)
elseif(PELEC_ENABLE_HIP)
  set(HYDRO_GPU_BACKEND HIP CACHE STRING "AMReX-Hydro GPU type" FORCE)
elseif(PELEC_ENABLE_DPCPP)
  set(HYDRO_GPU_BACKEND SYCL CACHE STRING "AMReX-Hydro GPU type" FORCE)
else()
  set(HYDRO_GPU_BACKEND NONE CACHE STRING "AMReX-Hydro GPU type" FORCE)
endif()
